# Services

Product-specific API entrypoints. Each service uses the shared `backend/` implementation.

- `services/autonomax_api`: Autonomax API entrypoint.
- `services/youtube_ai_api`: YouTube AI API entrypoint.
